{
  "name": "difftastic-crate",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "cargo watch -i README.md -i .gitignore -s \"wasm-pack build --target web --out-dir ../../packages/difftastic\"",
    "build": "wasm-pack build --target web --out-dir ../../packages/difftastic"
  }
}
